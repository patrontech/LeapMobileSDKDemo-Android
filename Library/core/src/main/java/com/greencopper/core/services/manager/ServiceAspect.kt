package com.greencopper.core.services.manager

/**
 * A ServiceAspect represents a use service that is managed by
 * the [ServiceManager].
 */
public data class ServiceAspect(private val name: String) {
    public companion object
}
