<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.KIBASDKPOC" parent="Theme.MaterialComponents.DayNight">
        <!-- Fallback dark background for edge-to-edge before SDK colors are applied -->
        <item name="android:windowBackground">@color/sdk_fallback_background</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>

    <style name="Theme.KIBASDKPOC.NoToolbar" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Status bar color. -->
        <item name="customStatusAndBottomBarColor">@color/status_and_bottom_bar_sdk_activity_dark</item>
    </style>
</resources>
